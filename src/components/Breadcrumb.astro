---
import type { BreadcrumbItem } from '../helpers/types';
interface Props { items: BreadcrumbItem[]; }
const { items } = Astro.props;
const all = [{ label: 'Acasă', href: '/' }, ...items];
---

<nav aria-label="Breadcrumb" class="py-4">
  <ol class="flex flex-wrap items-center gap-1 text-sm text-gray-400">
    {all.map((item, i) => (
      <li class="flex items-center gap-1">
        {i > 0 && <span class="text-gray-300">/</span>}
        {item.href && i < all.length - 1 ? (
          <a href={item.href} class="hover:text-pri-600 no-underline">{item.label}</a>
        ) : (
          <span class="text-gray-600 font-medium">{item.label}</span>
        )}
      </li>
    ))}
  </ol>
</nav>
